import React from "react";

const Result = ({ score }) => {
	return (
		<div className="bg">
			<h1 className="text-center text-light ">Score: {score}</h1>
		</div>
	);
};

export default Result;
